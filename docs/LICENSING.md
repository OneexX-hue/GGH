# Лицензии зависимостей

Проверено построчно на реальных LICENSE-файлах в этой сессии разработки
ТЗ (не по описаниям из поиска). Перепроверять при использовании — лицензии
меняются (пример: Matrix Synapse сменил Apache-2.0 на AGPL в 2023 году без
изменения кода).

**Правило для Claude Code:** перед `npm install` / `pip install` / любым
добавлением стороннего кода — искать пакет в таблице ниже. Если его нет —
самостоятельно найти и прочитать файл LICENSE в репозитории пакета,
добавить запись сюда, и только потом использовать. Не устанавливать
GPL/AGPL/сильный copyleft без явного одобрения человека — сообщить о
находке и подождать решения, а не устанавливать «на всякий случай, потом
уберём».

## ✅ Разрешено — можно использовать

| Компонент | Лицензия | Комментарий |
|---|---|---|
| `react-native-capture-protection` | MIT | Защита от скриншотов, актуально только при выборе React Native |
| `react-native-screenguard` | MIT | Резервный вариант к предыдущему |
| Zulip (сервер) | Apache-2.0 | Чат-бэкенд, кандидат — см. DECISIONS.md |
| Rocket.Chat (только код вне `ee/`) | MIT | Чат-бэкенд, кандидат. **Не включать `ee/`-директорию** |
| conduwuit (форк Matrix-сервера) | Apache-2.0 | Альтернатива Synapse, если решат остаться на протоколе Matrix |
| YOLOX (Megvii) | Apache-2.0 | Детектор для LPR, но проект не развивается с 2024 — проверить активные форки перед использованием |
| PostgreSQL | PostgreSQL License (permissive) | Основная БД |
| Redis | ⚠️ **не проверено в этой сессии** | По памяти лицензия менялась в последние годы (часть кода — RSAL) — обязательно проверить актуальный LICENSE-файл конкретной версии перед использованием, не полагаться на эту таблицу |

### Backend (NestJS), проверено 09.08.2026 по полю `license` в `package.json` каждого пакета

| Компонент | Лицензия |
|---|---|
| `@nestjs/*` (common, core, config, jwt, passport, platform-express, swagger, throttler, cli, schematics, testing) | MIT |
| `@prisma/client`, `prisma` | Apache-2.0 |
| `bcryptjs` | MIT |
| `class-transformer`, `class-validator` | MIT |
| `otplib` (2FA/TOTP) | MIT |
| `passport`, `passport-jwt` | MIT |
| `reflect-metadata` | Apache-2.0 |
| `rxjs` | Apache-2.0 |
| `typescript`, `ts-node`, `ts-jest`, `jest` | Apache-2.0 / MIT |

### Web-admin (Next.js), проверено 09.08.2026

| Компонент | Лицензия |
|---|---|
| `next`, `react`, `react-dom` | MIT |

### Mobile (Expo RN), проверено 09.08.2026

| Компонент | Лицензия |
|---|---|
| `expo`, `expo-dev-client`, `expo-status-bar` | MIT |
| `react-native` | MIT |
| `@react-navigation/native`, `@react-navigation/native-stack` | MIT |
| `react-native-screens`, `react-native-safe-area-context` | MIT |
| `@react-native-async-storage/async-storage` | MIT |
| `babel-preset-expo` | MIT |

Все зависимости Этапа 1 — permissive (MIT/Apache-2.0), GPL/AGPL не
обнаружено. `react-native-capture-protection`/`react-native-screenguard`
(уже проверены выше) в эту сессию не добавлялись в `package.json` —
будут подключены при реализации защиты медиа в чате (гл. 3.3 ТЗ).

## 🔴 Запрещено без покупки лицензии/явного одобрения

| Компонент | Лицензия | Почему нельзя просто так |
|---|---|---|
| Nomeroff Net | **GPL-3.0** | Сильный copyleft — по букве лицензии требует раскрытия кода всего приложения при встраивании |
| Ultralytics YOLOv8/v11/v12 | **AGPL-3.0** | Ultralytics официально подтвердила: даже офлайн-инференс без сети требует Enterprise-лицензию для закрытого коммерческого продукта |
| Matrix Synapse (сервер) | **AGPLv3** (с дек. 2023) | Нужна коммерческая лицензия Element для закрытого продукта |
| Любой пакет, зависящий от вышеперечисленных транзитивно | наследует ограничение | Проверять транзитивные зависимости, не только прямые |

## 🟡 Разрешено с оговорками — оценить риск сопровождения перед использованием

| Компонент | Лицензия | Оговорка |
|---|---|---|
| Oasis (`isuru89/oasis`) | Apache-2.0 | Лицензия ок, но проект незрелый (75 звёзд, «в разработке», 1 мейнтейнер) — см. DECISIONS.md, решено использовать только как референс архитектуры |

## Как добавлять новые записи

1. Найти официальный репозиторий пакета
2. Открыть файл `LICENSE` (не README — там иногда лицензия указана неточно, как выяснилось на примере Nomeroff Net)
3. Проверить, нет ли отдельной лицензии для части кода (пример: Rocket.Chat — MIT в целом, но `ee/`-директория отдельно)
4. Добавить строку в соответствующую таблицу выше с датой проверки
5. Если сомнение — переспросить пользователя, не решать самостоятельно в пользу «разрешено»
