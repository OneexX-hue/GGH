NEXT_PUBLIC_API_URL=http://localhost:3001

# TURN-сервер для WebRTC-звонков — опционально, без него используется
# только публичный Google STUN (см. docs/DECISIONS.md, "WebRTC-звонки").
# Self-hosted coturn поднимается через infra/docker-compose.yml.
NEXT_PUBLIC_TURN_URL=
NEXT_PUBLIC_TURN_USERNAME=
NEXT_PUBLIC_TURN_CREDENTIAL=
